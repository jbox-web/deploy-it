fr:

  label:
    docker_image:
      single: Image Docker
      plural: Images Docker
      new: Nouvelle image Docker

  notice:
    docker_image:
      created: Image Docker créée avec succès
      updated: Image Docker modifiée avec succès
      deleted: Image Docker supprimée avec succès

  activerecord:
    models:
      docker_image: image Docker

    attributes:
      docker_image:
        name: Nom
        description: Description

    errors:
      models:
        docker_image:
          attributes:
            name:
              blank: ne peut être nul
              taken: déjà pris
