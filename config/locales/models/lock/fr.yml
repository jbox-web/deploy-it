fr:

  label:
    lock:
      single: Verrou
      plural: Verrous

  notice:
    lock:
      deleted: Verrou supprimé avec succès

  activerecord:
    models:
      lock: verrou

    attributes:
      lock:
        token: Jeton

    errors:
      models:
        lock:
          attributes:
            token:
              blank: ne peut être nul
