fr:

  activerecord:
    icons:
      lock: fa-lock

    models:
      lock:
        one: Verrou
        other: Verrous

    attributes:
      lock:
        token: Jeton

    errors:
      models:
        lock:
          attributes:
            token:
              blank: ne peut être nul
