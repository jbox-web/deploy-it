fr:

  activerecord:
    icons:
      docker_image: fa-cubes

    models:
      docker_image:
        one: Image Docker
        other: Images Docker

    attributes:
      docker_image:
        name: Nom
        start_cmd: Commande de démarrage
        description: Description

    errors:
      models:
        docker_image:
          attributes:
            name:
              blank: ne peut être nul
              taken: déjà pris
            start_cmd:
              blank: ne peut être nul
