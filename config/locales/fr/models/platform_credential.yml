fr:

  activerecord:
    attributes:
      platform_credential:
        public_key: Clé publique
        private_key: Clé privée
        fingerprint: Empreinte

    models:
      platform_credential:
        one: "Clé d'accès"
        other: "Clés d'accès"

    errors:
      models:
        platform_credential:
          attributes:
            public_key:
              blank: ne peut être vide
            private_key:
              blank: ne peut être vide
            fingerprint:
              blank: ne peut être vide
