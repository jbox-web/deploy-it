$("#application_<%= @application.id %> span.action-status")
  .html('<%= render_application_state(@application.state) %>')
  .show();
<% if @after_action %>
$("#application_<%= @application.id %> span.action-result")
  .html('Done !')
  .show();
<% end %>
