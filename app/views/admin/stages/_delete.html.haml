%hr
%h4{ class: 'page-header' }= t('.destroy')
= link_to label_with_icon(t('button.delete'), 'fa-trash-o'),
          admin_platform_stage_path(stage.platform, stage),
          method: :delete,
          data: { confirm: t('text.are_you_sure') },
          class: "btn btn-danger"
